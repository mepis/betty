# Frontend Development Server
VITE_PORT=5173
VITE_HOST=0.0.0.0

# API URL — leave empty for relative URLs (same-origin when served by api-server)
# Set to an explicit URL only if deploying frontend separately from API
VITE_API_URL=
