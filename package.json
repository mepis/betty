{
  "name": "betty",
  "version": "1.0.0",
  "description": "Web-based chat app for Pi coding agent",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,magenta \"npm run backend\" \"npm run frontend\"",
    "backend": "cd src/backend && node server.js",
    "frontend": "cd src/frontend && npx vite --host",
    "build": "cd src/frontend && npx vite build",
    "start": "cd src/backend && node server.js",
    "install-all": "cd src/backend && npm install && cd ../frontend && npm install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.0.0",
    "playwright": "^1.60.0"
  }
}
